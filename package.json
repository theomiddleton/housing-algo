{
  "name": "housing-algo",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "assign": "bun run --cwd apps/cli index.ts",
    "residents": "bun run --cwd apps/cli lib/residents.ts",
    "rightmove": "bun run --cwd apps/cli lib/rightmove.ts"
  },
  "devDependencies": {
    "turbo": "^2.3.0"
  },
  "packageManager": "bun@1.1.0"
}
